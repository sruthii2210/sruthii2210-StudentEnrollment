import { StudentDetailsModel } from './student-details-model';

describe('StudentDetailsModel', () => {
  it('should create an instance', () => {
    expect(new StudentDetailsModel()).toBeTruthy();
  });
});
